# Vox Cambridge Reverb (V1031/V1032) — Electronics Rebuild

Complete electronics rebuild of a Vox Cambridge Reverb solid-state guitar amplifier:
the original electronics are gutted and replaced with modern parts while preserving
the original functionality, the front/rear panel layout (no new holes), and the
chassis. Guitar-frequency and 10″ alnico-speaker optimized throughout.

> **Honesty up front:** the Part 1–10 design docs were **reconstructed** from a prior
> conversation (see `docs/PROVENANCE.md`); some values were gap-filled. The KiCad
> project, BOM, and ngspice sims are **generated and tool-verified** (ERC/DRC/SPICE
> all pass), but the board is **placed, not routed**, and a few stages are designed
> substitutes. Treat the signal chain and power section as *validated design intent*,
> and re-verify part numbers/values before ordering or building.

## Core design decisions

| Block | Replacement |
|-------|-------------|
| Power amp | LM1875T (replaces germanium push-pull; eliminates driver transformer T2) |
| Preamp JFETs | MMBF5457 (SOT-23: direct on the SMD variant, on an adapter for THT) or J113 (2N5457 discontinued) — **availability watch**; pins 1 D / 2 S / 3 G (errata #18) |
| Reverb / tremolo | TL072 ×2 (no transformer T3): IC1 = tank driver + **active wet/dry summer**; IC2 = tremolo LFO + **output buffer** |
| Reverb pan | Accutronics 4FB2A1C (~1475 Ω input) — 8 Ω 4AB3C1B is **incompatible** |
| Tremolo modulation | Xvive VTL5C1 or DIY LED/LDR optocoupler, driven by a TL072 Wien LFO — **symmetric, dual-gang 250 k speed pot, 0.6–10.6 Hz** (errata #19: the recovered single-arm pot could not oscillate at tremolo rates) |
| Power | LM317T-regulated +17 V preamp rail (split `VBIAS_R`/`VBIAS_T` mid-rails); KBP410G bridge; `VREG_IN` RC pre-filter |
| Tone | passive Vox-style "cut" (designed substitute — original 25-5274-2 values not recovered) |

## What's verified (all via real tools, in CI-able commands)
- **Schematic:** generated by `kicad/gen/gen_kicad.py` → 8 hierarchical sheets, **ERC 0 violations** (KiCad 8.0.9). 117 components.
- **PCB:** `kicad/gen/gen_pcb.py` places every footprint into the **Part 5 floor-plan zones** (datasheet-derived LM1875 / KBP bridge footprints, real can sizes, Part 4 wire pads, mounting holes; **0 DRC errors, 0 warnings before routing**), plus a **labelled bench test-point strip per zone** (26 `TP_*`: rails, JFET drains, mid-rail refs, every inter-stage node, PA output, a GND pin per zone), then `kicad/gen/route_board.py` **autoroutes headlessly** with Freerouting. **167 of 168 connections routed (incl. the 26 test points), 3 DRC errors** (starved thermal spokes on three small capacitor pads), 49 vias, bottom layer kept as a near-solid GND pour (0.6 m of jumpers vs 4.1 m on top); the earlier placement reached 166/166 clean and the notes explain the variance. See `kicad/PCB-NOTES.md`.
- **Mixed SMD/THT variant** (`kicad/smd/`, **155×90 mm** for the Part 7 safe-bet chassis): same schematic through a second footprint profile — 70 small parts on 0805/1206/1210/SMA/SOD-123/SOT-23, power parts / electrolytics / socketed op-amps / trim resistors / connectors stay THT. **162 of 169 connections routed, 0 DRC violations** on the 155×90 board; the seven open links (mostly ground stitching) are listed in the notes. JLCPCB-assembly BOM + position file in `production/smd/`.
- **BOM:** generated from the schematic by `kicad/gen/gen_bom.py` (1:1 with the netlist, no drift) → `bom/bom.csv` + `bom/bom-grouped.csv` (THT) and `bom/bom-smd*.csv` (variant).
- **SPICE:** `spice/run_all.sh` runs 8 ngspice block checks (preamp bias, reverb driver, MRB, tremolo LFO, power amp, **rail-aware** reverb summer, tone, Class-A stage) **plus 6 parameter sweeps** (JFET bias vs R_s across the datasheet Idss spread, tone pot, LFO speed pot — recovered and fixed networks, Class-A bias, power-amp headroom vs mains ±10 %) and saves every result table to **`spice/results/`** (committed evidence).

## Honest engineering notes (from the audit/roast passes — see `docs/roast-2026-06-16.md`)
- 🔴 **Output power ≈ 12 W, not 18 W.** A single 33.5 V rail can't swing the 17 V peak that 18 W into 8 Ω needs; the LM1875 ceilings at ~12 W clean (~1.8 dB down). Fine for a 10″ combo.
- 🔴 **Fit a MAINS (primary) fuse** — the design's other fuse is only on the secondary. Required for safety.
- ⚠️ **Chassis fit:** the board is **190×115 mm** (matches the original 25-5274-2). Real (text-less) packing is **~33 %** there and **~56 %** on the Part 7 155×90 "safe-bet" — the earlier 53 % / 90 % figures counted hidden text. Measure the real chassis before committing to a size (errata #9); the placer rebalances to any outline.

## Key constraints
- No new holes in original panels; all improvements internal only.
- MRB hardwired to 600 Hz (original spec); tremolo rate LED is PCB-mounted (diagnostic).
- Original 6-pin DIN footswitch connector and pedal retained.

## Repository layout

```
docs/         Parts 1–10, errata, PROVENANCE, audits (component-availability, roast)
kicad/        Generated KiCad project + gen/ scripts; symbols/ footprints/ gerbers/;
              SCHEMATIC-BUILD / PCB-NOTES / PCB-AUDIT
spice/        ngspice block sims + run_all.sh + models/ (LTspice power-amp kept for provenance)
bom/          bom.csv (generated), bom-grouped.csv, cross-check
datasheets/   sourcing pointers (SOURCES.md)   photos/  capture checklist
production/   fab-package checklist
```

## Tools
- **EDA / verification:** KiCad 8 (`kicad-cli` ERC/DRC). `kicad/gen/check.sh tht|smd` regenerates a profile and writes ERC/DRC reports to `kicad/reports/` (add `--no-regen` to verify a routed board); route with `python3 kicad/gen/route_board.py` (Freerouting 1.9.0 jar + Java + xvfb, see the script)
- **Datasheets:** `datasheets/` holds the manufacturer PDFs the footprints and pinouts were taken from, with a manifest (`SOURCES.md`: file, revision, checksum, which decision it backs)
- **Simulation:** ngspice (`spice/run_all.sh`); LTspice netlist kept for the power amp
- **PCB fab / sourcing:** JLCPCB · Mouser / Digikey

## Status
Reconstructed Parts 1–10 + errata, then multiple verify/audit/roast passes:
generated ERC-clean schematic, floor-plan-placed and **autorouted** PCB, generated
BOM, runnable ngspice suite, and circuit fixes (mains fuse, power-ceiling
correction, active effects-chain redesign, split bias, tone stack).
**Remaining:** hand-review of the autorouted copper (audio-sensitive nets, star
ground, the fat power loop), chassis measurement → final outline, Gerbers, and
bench-tuning the tremolo/MRB. See `CHANGELOG.md`.
